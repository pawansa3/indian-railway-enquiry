extends layout

block content
    .container.mycontr
        .row
            include ./partials/_side-bar.pug
            include ./partials/_main-content.pug
                block main-content
                    .main-block
                        h3.text-center.bg-primary.myheading Train Schedule
                        h4.text-center
                            | Check Train Schedule
                        form.form-horizontal(action='#')
                            .form-group
                                label.control-label.col-sm-3(for='') Enter Train Number:
                                .col-sm-9
                                    input.form-control(type='text', placeholder='Enter Train no.', name='')
                            button.btn.btn-primary.my-btn(type='submit') Get Status
                            |  
                            button.btn.btn-danger.my-btn(type='reset') Clear